(function(root, factory) {
  const api = factory();
  if (typeof module === 'object' && module.exports) module.exports = api;
  root.TranscriptSearch = api;
})(typeof globalThis !== 'undefined' ? globalThis : this, function() {
  const stop = new Set('the and for that with this from have are was were will would could should into about your our their they them then than been being not but you all can may has had its his her she he we of to in on at by as is it or be do did does if so no yes what when where who how why which out up down over under more most some any each other there here also just very much many such only through because while during before after between within without across per via upon these those'.split(' '));
  const tokens = q => String(q || '').toLowerCase().replace(/[’‘]/g, "'").match(/[a-z0-9']{3,}/g)?.filter(t => !stop.has(t) && !/^[0-9]+$/.test(t)) || [];
  const canon=s=>String(s||'').toLowerCase().replace(/[’‘']/g,'').replace(/[^a-z0-9]/g,'');
  function editDistance(a,b){a=canon(a);b=canon(b);const prev=Array.from({length:b.length+1},(_,i)=>i);for(let i=1;i<=a.length;i++){let left=i,diag=i-1;for(let j=1;j<=b.length;j++){const up=prev[j],v=a[i-1]===b[j-1]?diag:1+Math.min(diag,left,up);diag=up;prev[j]=v;left=v}}return prev[b.length]}
  function fuzzyTokenMatch(a,b){a=canon(a);b=canon(b);if(!a||!b)return false;if(a===b)return true;if(Math.min(a.length,b.length)<5)return false;const d=editDistance(a,b),limit=Math.max(a.length,b.length)>=8?2:1;return d<=limit}
  const decodePostings = s => String(s || '').split('.').filter(Boolean).map(x => parseInt(x, 36)).filter(Number.isFinite);
  function postingsFor(index,t){const exact=decodePostings(index&&index.x&&index.x[t]);if(exact.length)return exact;const keys=Object.keys(index?.x||{}),matches=keys.filter(k=>fuzzyTokenMatch(t,k));if(!matches.length)return[];return [...new Set(matches.flatMap(k=>decodePostings(index.x[k])))]}
  function searchIndex(index, q) {const ts=[...new Set(tokens(q))];if(!ts.length)return[];const lists=[];for(const t of ts){const p=postingsFor(index,t);if(!p.length)return[];lists.push(p)}lists.sort((a,b)=>a.length-b.length);let ids=new Set(lists[0]);for(const list of lists.slice(1)){const s=new Set(list);ids=new Set([...ids].filter(i=>s.has(i)));if(!ids.size)break}return [...ids].map(i=>index.r[i]).filter(Boolean).sort((a,b)=>String(b[1]||'').localeCompare(String(a[1]||'')))}
  function formatTimestamp(seconds){const n=Math.max(0,Math.floor(Number(seconds)||0)),h=Math.floor(n/3600),m=Math.floor((n%3600)/60),s=n%60;return h?`${h}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`:`${m}:${String(s).padStart(2,'0')}`}
  function textHasToken(text,t){return tokens(text).some(w=>fuzzyTokenMatch(t,w))}
  function findMatches(segments,q){const wanted=[...new Set(tokens(q))];if(!wanted.length||!Array.isArray(segments))return[];const make=(i,count)=>{const text=segments.slice(i,i+count).map(s=>String(s?.[1]||'')).join(' ').trim(),before=i?String(segments[i-1]?.[1]||'')+' ':'',after=i+count<segments.length?' '+String(segments[i+count]?.[1]||''):'';return{start:Number(segments[i]?.[0])||0,excerpt:(before+text+after).trim()}};const direct=[];for(let i=0;i<segments.length;i++){const hay=String(segments[i]?.[1]||'');if(wanted.every(t=>textHasToken(hay,t)))direct.push(make(i,1))}if(direct.length)return direct;const hits=[];for(let i=0;i<segments.length;i++){for(const count of [2,3]){const hay=segments.slice(i,i+count).map(s=>s?.[1]||'').join(' ');if(wanted.every(t=>textHasToken(hay,t))){hits.push(make(i,count));i+=count-1;break}}}return hits}
  async function loadArchive(base='data/transcript-index'){const plain=await fetch(`${base}/RBBCSC_transcript_index.json`,{cache:'no-cache'});if(!plain.ok)throw new Error('Transcript index failed to load');const index=await plain.json();if(!index||!Array.isArray(index.r)||!index.x)throw new Error('Transcript archive index is invalid');return index}
  async function loadTranscriptData(url='data/RBBCSC_transcripts.json.gz'){const r=await fetch(url,{cache:'no-cache'});if(!r.ok)throw new Error('Transcript detail data failed to load');if(typeof DecompressionStream!=='function')throw new Error('This browser cannot open compressed transcript detail data');const stream=r.body.pipeThrough(new DecompressionStream('gzip'));const text=await new Response(stream).text(),data=JSON.parse(text);if(!Array.isArray(data))throw new Error('Transcript detail data is invalid');return data}
  const findMeeting=(data,catsId)=>Array.isArray(data)?data.find(r=>String(r?.[0])===String(catsId)):null;let detailPromise=null;const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  async function enrichCards(){if(typeof document==='undefined')return;const query=document.getElementById('q')?.value?.trim()||'';if(!query)return;const cards=[...document.querySelectorAll('#results .card')].filter(c=>c.querySelector('a[href*="catstv.net/m.php?q="]')&&!c.dataset.transcriptEnriched);if(!cards.length)return;try{detailPromise=detailPromise||loadTranscriptData();const data=await detailPromise;if(query!==(document.getElementById('q')?.value?.trim()||''))return;for(const card of cards){const a=card.querySelector('a[href*="catstv.net/m.php?q="]'),id=new URL(a.href).searchParams.get('q'),row=findMeeting(data,id);if(!row)continue;const hits=findMatches(row[3],query);if(!hits.length)continue;const hit=hits[0],p=card.querySelector('.excerpt');if(p)p.innerHTML=esc(hit.excerpt);const info=document.createElement('p');info.style.fontWeight='900';info.textContent=`First transcript match: ${formatTimestamp(hit.start)}`;p?.after(info);const view=document.createElement('a');view.href=`transcript-view.html?id=${encodeURIComponent(id)}&q=${encodeURIComponent(query)}`;view.textContent='View Full Transcript ↗';view.style.marginRight='18px';a.before(view);card.dataset.transcriptEnriched='1'}}catch(e){console.error('Transcript detail enrichment failed',e)}}
  function installEnricher(){if(typeof document==='undefined')return;const start=()=>{const r=document.getElementById('results');if(!r)return;new MutationObserver(()=>setTimeout(enrichCards,0)).observe(r,{childList:true});document.getElementById('go')?.addEventListener('click',()=>setTimeout(enrichCards,100));setTimeout(enrichCards,100)};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start):start()}
  installEnricher();return{tokens,editDistance,fuzzyTokenMatch,decodePostings,searchIndex,formatTimestamp,findMatches,loadArchive,loadTranscriptData,findMeeting};
});
