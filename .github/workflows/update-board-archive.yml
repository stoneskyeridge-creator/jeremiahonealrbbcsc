name: Update RBBCSC board archive

on:
  workflow_dispatch:
  pull_request:
  push:
    paths:
      - 'scripts/build-board-archive.js'
  schedule:
    - cron: '17 9 * * *'

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install --no-audit --no-fund
      - name: Test board archive/search code
        run: node --test tests/board-archive.test.js tests/board-search-aliases.test.js tests/board-search.test.js
      - run: npm run build:board-archive
      - name: Re-test board archive/search code
        run: node --test tests/board-archive.test.js tests/board-search-aliases.test.js tests/board-search.test.js
      - name: Validate built archive search
        run: node scripts/validate-board-archive.js
      - name: Commit refreshed archive
        run: |
          if git diff --quiet -- data/search-index.json data/search-shards data/meetings.json data/transcript-index; then
            echo "No archive changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A data/search-index.json data/search-shards data/meetings.json data/transcript-index
          git commit -m "data: refresh RBBCSC board archive"
          git push
